/** Keep in sync with $fixora-bp-phone in scss/variables.scss (DECISIONS MOB-03) */
export const FIXORA_BP_PHONE_PX = 639;

/** Admin console drawer layout — matches admin.scss @media (max-width: 1023px) */
export const FIXORA_ADMIN_MOBILE_PX = 1023;

export const FIXORA_MOBILE_MEDIA_QUERY = `(max-width: ${FIXORA_BP_PHONE_PX}px)`;
export const FIXORA_ADMIN_MOBILE_MEDIA_QUERY = `(max-width: ${FIXORA_ADMIN_MOBILE_PX}px)`;
